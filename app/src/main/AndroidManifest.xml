<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".activities.ReviewDetails"
            android:exported="false" />
        <activity
            android:name=".activities.FetchingReview"
            android:exported="false" />

        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />

        <activity
            android:name=".activities.InsertionReview"
            android:exported="false" />
        <activity
            android:name=".activities.RWMainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.availableDetailsActivity" />
        <activity android:name=".activities.availableJobFetchingActivity" />
        <activity android:name=".activities.jobFetchingActivity" />
        <activity android:name=".activities.jobInsertionActivity" />
        <activity android:name=".activities.postedDetailsActivity" />
    </application>

</manifest>